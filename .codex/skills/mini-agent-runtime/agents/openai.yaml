interface:
  display_name: "Mini Agent Runtime"
  short_description: "Project rules for the Go agent runtime."
  default_prompt: "Use $mini-agent-runtime before changing this Go agent runtime repository."
policy:
  allow_implicit_invocation: true
